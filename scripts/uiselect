#!/bin/sh

urxvt -name "FLOAT"\
    -fn "xft:FantasqueSansMono Nerd Font Mono:size=14" \
    -e sh -c "cat $0 | fzf --color=16 --info=inline --layout=reverse --border=rounded < /proc/$$/fd/0 > /proc/$$/fd/1"

